# Port the CSMS listens on (Railway sets PORT automatically; locally defaults to 9000)
PORT=9000

# --- Next step: PostgreSQL (not wired yet — see db/schema.sql) ---
# DATABASE_URL=postgresql://user:pass@host:5432/evcharger
